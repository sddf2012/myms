spring:
  application:
    name: business-club
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        service-id: base-configCenter
      name: common,business-club

server:
  port: 8000

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
    fetch-registry: true
    register-with-eureka: true